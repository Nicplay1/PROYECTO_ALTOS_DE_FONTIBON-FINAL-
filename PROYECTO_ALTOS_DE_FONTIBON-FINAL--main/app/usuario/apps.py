from django.apps import AppConfig


class UsuarioConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app.usuario'   # ✅ nombre completo de la app dentro del proyecto
